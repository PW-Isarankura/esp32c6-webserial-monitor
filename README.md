# ESP32-C6 Web Serial Monitor 🚀

ระบบตรวจสอบข้อมูลจาก Serial Port แบบ Real-time ผ่านหน้าเว็บเบราว์เซอร์ สำหรับบอร์ด **ESP32-C6** โปรเจกต์นี้เน้นความเสถียรบนมาตรฐาน WiFi 6 และการแยกส่วนแสดงผล (HTML) กับส่วนตั้งค่า (JSON) ออกจากโค้ดหลัก

## ✨ รายละเอียดโปรเจกต์ (Project Overview)
โปรเจกต์นี้ใช้ **Standard WebServer** ร่วมกับ **WebSockets** เพื่อส่งข้อมูลจาก Serial Port ไปยัง Dashboard บนหน้าเว็บแบบ Dynamic ข้อมูลจะถูกส่งแบบ Full-duplex ทำให้เห็นข้อมูลวิ่งแบบ Real-time โดยไม่ต้อง Refresh หน้าจอ

## 🛠 สิ่งที่ต้องเตรียม (Prerequisites)
### Libraries ที่ต้องใช้:
- **ArduinoJson** (โดย Benoit Blanchon): สำหรับอ่านค่าจากไฟล์ `config.json`
- **WebSockets** (โดย Markus Sattler): สำหรับการเชื่อมต่อข้อมูลแบบ Real-time

### การตั้งค่าใน Arduino IDE 2.x:
- **Board**: `ESP32C6 Dev Module`
- **USB CDC On Boot**: `Enabled` (เพื่อให้ Serial พิมพ์ออกทาง USB ได้)
- **Flash Size** เลือกให้ตรงกับขนาด Flash ของบอร์ดที่ใช้ 
- **Partition Scheme**: เลือกที่มี LittleFS (เช่น `8M Flash (3MB APP/1.5MB LittleFS)`)
โค้ดในโปรเจตจะใช้ Custom Partition ซึ่งระบบเรียกใช้ไฟล์ `partitions.csv` ในโฟลเดอร์โปรเจกต์ หากผู้ใช้บอร์ดมี Flash Size อื่นๆที่ไม่ใช่ N16 จำเป็นต้องแก้ไข partitions.csv ให้เหมาะสมกับบอร์ดตัวเอง

---

## 📤 การติดตั้ง LittleFS Upload Plugin (สำหรับ IDE 2.x)
เนื่องจาก Arduino IDE 2.x ไม่มีเครื่องมือ Upload ไฟล์มาให้ในตัวเหมือนรุ่น 1.x คุณต้องติดตั้ง Plugin เพิ่มเติมดังนี้:

1. **ดาวน์โหลด**: ไปที่ [arduino-littlefs-upload](https://github.com/earlephilhower/arduino-littlefs-upload?tab=readme-ov-file) และดาวน์โหลดไฟล์ arduino-littlefs-upload-1.x.x.vsix
2. **การติดตั้ง:** นำไฟล์ไปวางในโฟลเดอร์ Plugin ของ Arduino IDE 
   - (โดยปกติอยู่ที่ `C:\Users\<YourName>\.arduinoIDE\plugins`)
3. **การใช้งาน:**
   - **ปิด Serial Monitor ก่อนทุกครั้ง** (เพื่อไม่ให้ Port ไม่ว่าง)
   - กดปุ่ม `Ctrl + Shift + P` (Command Palette)
   - พิมพ์คำว่า **Upload LittleFS to Pico/ESP32** แล้วกด Enter

---

## 🚀 ขั้นตอนการใช้งาน (Quick Start)
1. แก้ไขไฟล์ `data/config.json` ใส่ชื่อ WiFi และรหัสผ่านของคุณ
2. แก้ไขไฟล์ `data/index.html` ในส่วนของ WebSocket URL ให้ตรงกับ Port ที่ตั้งไว้ (Default: 81)
3. กด **Upload Data (LittleFS)** เพื่อส่งไฟล์ config และ html เข้าบอร์ด
4. กด **Upload Code** เพื่อรันโปรแกรมหลัก
5. เปิด **Serial Monitor** เพื่อดู IP Address ที่ได้รับจาก Router
6. เปิดเว็บเบราว์เซอร์แล้วพิมพ์ IP Address นั้น (เช่น `http://192.168.1.50`) เพื่อเริ่มใช้งาน

---

## 📂 โครงสร้างโฟลเดอร์
```text
esp32c6-webserial-monitor/
├── esp32c6-webserial-monitor.ino  # โค้ดหลัก
├── data/                          # ไฟล์สำหรับ LittleFS
│   ├── index.html                 # หน้าเว็บ UI
│   └── config.json                # ไฟล์ตั้งค่า WiFi/Port
└── README.md
```
---
**Developed by:** Phawat Isarankura Na Ayudhya
